#!/usr/bin/env bash
#MISE description="Clear stored authentication tokens"
set -eo pipefail

source "$MISE_CONFIG_ROOT/lib/graph.sh"

if [ -f "$TOKEN_FILE" ]; then
  rm "$TOKEN_FILE"
  echo "Logged out. Tokens cleared."
else
  echo "No tokens found — already logged out."
fi
